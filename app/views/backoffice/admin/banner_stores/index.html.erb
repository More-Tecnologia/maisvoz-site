<%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
<!-- Page-Title -->
<div class="box-orders">
  <div class="box-second">
    <h1 class="page-title m-b-30"><%= t(:banner_stores) %></h1>
  </div>
</div>
<div class="box-orders">
  <div class="box-second">
    <div class="card-box">
      <div class="row">
        <div class="col-lg-12">
            <div class="table-responsive">
              <table class="table table-striped table-bordered table-hover m-0">
                <thead>
                  <tr>
                    <th>#</th>
                    <th><%= t('title') %></th>
                    <th><%= t('backoffice.tickets.index.image_path') %></th>
                    <th><%= t(:active?) %></th>
                    <th><%= t('backoffice.tickets.index.action') %></th>
                  </tr>
                </thead>
                <tbody>
                  <% @banner_stores.each do |banner| %>
                  <tr>
                    <td><%= banner.id %></td>
                    <td>
                      <a href="<%= banner.title %>" target="_blank"><%= banner.title %></a>
                    </td>
                    <td>
                      <a href="<%= banner.path %>" target="_blank"><%= banner.path %></a>
                    </td>
                    <td><%= t(banner.active) %></td>
                    <td>
                      <%= link_to edit_backoffice_admin_banner_store_path(banner), class: 'action-edit' do %>
                        <i class="fa fa-edit"></i>
                      <% end %>
                      <%= link_to(backoffice_admin_banner_store_path(banner),
                                  class: 'action-remove',
                                  method: :delete,
                                  data: { confirm: t(:banner_store_inactivation_message) }
                                  ) do %>
                        <i class="fa fa-minus-circle"></i>
                      <% end %>
                    </td>
                  </tr>
                <% end %>
                </tbody>
              </table>

            </div>
        </div>
          <br>
          <div class="col-md-10">
            <%= paginate @banner_stores %>
          </div>
          <div class="col-md-2">
            <br>
            <%= link_to t(:new), new_backoffice_admin_banner_store_path, class: 'btn btn-success pull-right' %>
          </div>

    </div>
  </div>
</div>
