<p><%= t(:hello) %> <%= @resource.email %>!</p>

<p><%= t(:mail_locked_text) %></p>

<p><%= t(:mail_locked_ins) %></p>

<p><%= link_to t(:mail_unlock_account), unlock_url(@resource, unlock_token: @token) %></p>
