<%= link_to shop_index_url(sponsor: current_user.username), class: 'nav-link waves-effect' do %>
  <i class="fa fa-link"></i> <%= shop_index_url(sponsor: current_user.username, locale: '').remove('locale=&') %>
<% end %>
